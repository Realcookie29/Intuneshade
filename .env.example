# Copy this file to .env.local for local development only.
# In production (Azure Static Web Apps), set these in the portal under Configuration > Application settings.
# The CLIENT_ID is your Azure App Registration Application (client) ID.
# It is safe to commit to source control — there is no secret in this SPA.
VITE_CLIENT_ID=your-app-registration-client-id-here
